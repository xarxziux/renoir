/*
const throwError = message => {throw new Error (message)};

const push = val => ({
    
    newNode ()
    
});

const peek = node => () => node.val;

const emptyNode = {
    
    peek: () => null,
    pop: () => throwError ('Illegal pop() call on an empty node'),
    
};

/*
const getNonEmptyNode = val => ({
    
    peek:
    
});

const getEmptyNode = () => ({
    
    peek:
    
});

const newNode = val
*/
