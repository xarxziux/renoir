module.exports = {
    presets: [
        ['env', {
            // modules: false,
            // loose: true,
            targets: {
                browsers: ['IE 11']
            }
        }]
    ]
}
